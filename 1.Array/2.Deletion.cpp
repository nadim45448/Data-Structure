#include<iostream>
using namespace std;
int main()
{
    /*int arr[15];
    int sizee;
    cout<<"Enter array size:";
        cin>>sizee;
        cout<<"Enter array elements:"<<endl;
        for(int i=0;i<sizee;i++)
        {
            cin>>arr[i];
        }
        cout<<"Array before deletion: ";
        for(int i=0;i<sizee;i++)
    {
        cout<<arr[i]<<" ";
    }
    cout<<endl;
    // deletion
    int pos;
    cout<<"Enter position:";
    cin>>pos;
    if(pos<0 ||pos>sizee-1)
    {
        cout<<"Invalid Index!"<<endl;
    }
    else
    {
        if(pos==sizee-1)
        {
            sizee--; //last index deletion
        }
        else
        {
            for(int i=pos+1;i<sizee;i++) //1st or middle index deletion
         {
            arr[i-1]=arr[i];
         }
         sizee--;
        }


    }
    cout<<"after deletion:";
         for(int i=0;i<sizee;i++)
    {
        cout<<arr[i]<<" ";
    } */
    int a[3]={1,2};
    cout<<a[2];

}
