#include <bits/stdc++.h>
#include "2.stackUsingDoublyLinkedList.h"
using namespace std;
int main()
{
    Stack st;
   
    //cout<<"value "<<st.pop()<<" popped"<<endl;

    st.push(1);
     st.pop();
    st.push(2);
    st.push(3);
    st.pop();
     st.pop();
      st.pop();
       st.pop();



    return 0;
    
}